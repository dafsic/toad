// TODO: 订单列表组件
// 展示列：id, exchange, side, quantity, price, price_change, status, filled_price, is_auto, created_at
// 每行提供取消按钮（status='open' 时可用），调用 api.cancelOrder(id)
// 支持单个取消与批量取消（checkbox 多选）

export default function OrderList() {
    return <div>OrderList — TODO</div>
}
